%YAML 1.1
---
"$schema": "http://json-schema.org/draft-06/schema#"
title: ctdb daemon role
description: |
  ctdb daemon, server role
type: object
additionalProperties: false

required:
  - enabled

properties:
  enabled:
    description: |
      Enables ctdb daemon service
    type: boolean
